#include <stdio.h>

void hsort(void *base, size_t nel, size_t width,
        int (*compare)(const void *a, const void *b)) {
    
}

int main(int argc, char **argv) {
    size_t mas_size, el_size;
    
    return 0;
}